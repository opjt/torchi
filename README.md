# puno

Puno는 웹훅(Webhook)이나 API를 통해 모바일로 실시간 푸시 알림을 보내고, 알림에서 바로 **액션(수락/거절/응답 등)** 을 취할 수 있는 가볍고 개발자 친화적인 알림 플랫폼입니다.

## 개발 단계

1. MVP 구현
   - webhook/API -> push 알림
   - PWA로 구현. 추후 앱 개발
   - 간단한 상호작용 구현(accept,reject), pull 방식으로 리턴값 받도록
